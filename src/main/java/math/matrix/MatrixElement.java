package math.matrix;
import java.awt.*;

public class MatrixElement {
    public Point index;
    public Double value;

    @Override
    public String toString() {
        return "MatrixElement{" + "index=" + index + ", value=" + value + '}';
    }
}
